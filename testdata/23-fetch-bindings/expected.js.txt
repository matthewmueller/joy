(function() {
  var pkgs = {}
  pkgs['input.go'] = (function() {
    async function main() {
      var res, err
      try {
        res = await window.fetch("http://google.com", {
          method: "POST"
        })
      } catch ($e) {
        err = $e
      }
      if (err) {
        console.log(err)
        return
      }

      var raw, err
      try {
        raw = await res.json()
      } catch ($e) {
        err = $e
      }
      if (err) {
        console.log(err)
        return
      }
      
      console.log(raw)
    }

    return {
      main: main
    }
  })()
  pkgs['input.go'].main()
})()